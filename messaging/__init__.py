"""Платформо-независимый слой мессенджеров.

Единый API для работы с ботами поверх разных платформ (Telegram, VK).
Хэндлеры бизнес-логики живут в `messaging.handlers` и работают только
с контрактами из `messaging.contracts` — они не знают, какая платформа
активна. Выбор платформы — через `messaging.factory.get_platform()`.
"""
