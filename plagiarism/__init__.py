"""Антиплагиат: метод шинглов + семантическое сравнение через BERT.

Пакет построен так, чтобы тяжёлая инициализация BERT происходила лениво
(только при первом вызове). Запуск проверки идёт через Celery-task, а
результат сохраняется в `PlagiarismReport`.
"""

default_app_config = 'plagiarism.apps.PlagiarismConfig'
